# Архитектура DeFi APY Bot

## Общая структура проекта

```
defi-apy-bot/
├── bot/                          # Основной модуль бота
│   ├── __init__.py
│   ├── main.py                   # Точка входа, инициализация бота и роутеров
│   │
│   ├── handlers/                 # Обработчики команд и callback'ов
│   │   ├── __init__.py
│   │   ├── start.py              # Команда /start, главное меню
│   │   ├── enhanced.py           # Основные команды: /stats, /pools, /farm, /top, /search, /pool
│   │   └── help.py               # Команда /help, справка
│   │
│   ├── adapters/                 # Адаптеры для работы с внешними API (устаревшие)
│   │   ├── __init__.py
│   │   ├── base.py               # Базовый класс адаптера, PoolData dataclass
│   │   └── hyperion.py           # Адаптер Hyperion API (не используется в новой версии)
│   │
│   ├── database/                 # Работа с базой данных
│   │   ├── __init__.py
│   │   ├── models.py             # SQLAlchemy модели (User, Pool, WatchedPool)
│   │   └── crud.py               # CRUD операции для работы с БД
│   │
│   └── utils/                    # Вспомогательные утилиты
│       ├── __init__.py
│       ├── hyperion_enhanced.py  # HyperionAPI класс - основной API клиент с кэшированием
│       ├── telegram_formatter.py # TelegramFormatter - форматирование сообщений
│       ├── fee_tier.py           # Утилиты для работы с Fee Tiers
│       └── token_registry.py     # Реестр токенов, парсинг символов
│
├── config/                       # Конфигурация приложения
│   ├── __init__.py
│   └── settings.py               # Настройки через pydantic-settings
│
├── logs/                         # Логи приложения
├── data/                         # База данных SQLite
│
├── requirements.txt              # Зависимости Python
├── .env.example                  # Пример файла с переменными окружения
├── .gitignore                    # Git ignore правила
├── README.md                     # Документация проекта
└── ARCHITECTURE.md               # Этот файл
```

## Компоненты архитектуры

### 1. Точка входа (`bot/main.py`)

**Роль:** Инициализация бота, регистрация роутеров, запуск polling

**Основные функции:**
- Создание экземпляра Bot и Dispatcher (aiogram 3.x)
- Регистрация роутеров из handlers
- Инициализация БД при старте
- Graceful shutdown

**Роутеры (в порядке регистрации):**
1. `start.router` - команда /start, главное меню
2. `help.router` - команда /help
3. `enhanced.router` - все основные команды

### 2. Handlers (Обработчики)

#### `bot/handlers/start.py`
- **Команда `/start`** - приветствие, регистрация пользователя
- **Главное меню** - `ReplyKeyboardMarkup` с кнопками:
  - 🏠 Главное меню
  - 🔷 Выбрать блокчейн
  - 📚 Справка
- Обработчики текстовых сообщений от кнопок меню

#### `bot/handlers/enhanced.py`
**Основные команды:**
- `/stats` - Market Overview (статистика рынка)
- `/pools` - Топ 10 пулов по TVL
- `/farm` - Пулы с farming
- `/top [tvl|volume|apr|fees]` - Топ пулов по метрике
- `/search <token>` - Поиск пулов по токену
- `/pool <token_a>-<token_b>` - Детальная информация о пуле

**Callback handlers:**
- `select_blockchain_aptos` - выбор блокчейна Aptos (показ протоколов)
- `select_protocol_hyperion` - выбор протокола Hyperion (показ пулов)
- `show_pools_hyperion` - показ пулов Hyperion
- `refresh_stats`, `refresh_pools` - обновление данных
- `filter_farm` - фильтр по farming
- `sort_*` - сортировка по метрикам
- `refresh_pool_*` - обновление отдельного пула

**Inline клавиатуры:**
- Фильтры и сортировка пулов
- Навигация между экранами
- Кнопки "Назад", "Refresh"

#### `bot/handlers/help.py`
- `/help` - подробная справка по командам
- `/commands` - краткий список команд

### 3. API и данные (`bot/utils/hyperion_enhanced.py`)

**Класс `HyperionAPI`:**
- **Кэширование:** TTL 60 секунд
- **Методы:**
  - `get_all_pools()` - получение всех пулов с фильтрацией (min TVL $100k, min Volume $50k)
  - `get_market_stats()` - вычисление статистики рынка
  - `filter_pools()` - фильтрация и сортировка пулов
  - `_fetch_pools_from_api()` - запрос к GraphQL API
  - `_enrich_pool()` - обогащение данных пула

**Dataclass `MarketStats`:**
- total_value_locked
- cumulative_volume
- volume_24h
- capital_efficiency

### 4. Форматирование (`bot/utils/telegram_formatter.py`)

**Класс `TelegramFormatter`:**
- `format_market_overview()` - Market Overview
- `format_protocol_stats()` - статистика протокола (TVL, Volume, Fees)
- `format_pools_table()` - таблица пулов (топ 10)
- `format_pool_detail()` - детальная информация о пуле
- `format_farm_pools()` - пулы с farming

### 5. Реестр токенов (`bot/utils/token_registry.py`)

**Функции:**
- `get_token_symbol()` - универсальная функция получения символа токена
- `parse_token_symbol_from_address()` - парсинг символа из Move адреса
- `TOKEN_REGISTRY` - словарь известных токенов (21 токен)
- `TOKEN_PATTERNS` - регулярные выражения для распознавания

**Процесс определения символа:**
1. Проверка в TOKEN_REGISTRY (точное совпадение)
2. Проверка по TOKEN_PATTERNS (regex)
3. Парсинг из адреса (извлечение из структуры Move адреса)

### 6. Fee Tiers (`bot/utils/fee_tier.py`)

**Функции:**
- `format_fee_tier()` - конвертация feeRate в процент (100 → 0.01%)
- `get_fee_tier_category()` - определение категории (Ultra Low, Low, Medium, High)
- `get_fee_tier_description()` - полное описание с категорией
- `calculate_fees_from_volume()` - вычисление fees из volume и fee rate

### 7. База данных (`bot/database/`)

**Модели (`models.py`):**
- `User` - пользователи бота (telegram_id, username, фильтры)
- `Pool` - пулы ликвидности (адрес, токены, метрики, fee_rate)
- `WatchedPool` - отслеживаемые пулы пользователями

**CRUD операции (`crud.py`):**
- `init_db()` - инициализация БД (создание таблиц)
- `get_or_create_user()` - получить/создать пользователя
- `upsert_pool()` - создать/обновить пул
- `get_top_pools()` - получить топ пулов с фильтрами
- `get_pool_by_address()` - найти пул по адресу

**База данных:** SQLite (по умолчанию) или PostgreSQL

### 8. Конфигурация (`config/settings.py`)

**Класс `Settings` (pydantic-settings):**
- `BOT_TOKEN` - токен Telegram бота
- `DATABASE_URL` - URL базы данных (по умолчанию SQLite)
- `APTOS_GRAPHQL_URL` - URL Aptos GraphQL Indexer
- `APTOS_NETWORK` - сеть (mainnet)

Загрузка из `.env` файла

## Поток данных

### 1. Инициализация бота
```
main.py → init_db() → создание таблиц
      → создание Bot и Dispatcher
      → регистрация роутеров
      → start_polling()
```

### 2. Обработка команды пользователя
```
Пользователь → Telegram → Bot → Router → Handler
                                          ↓
                                    HyperionAPI.get_all_pools()
                                          ↓
                                    TelegramFormatter.format_*()
                                          ↓
                                    Ответ пользователю
```

### 3. Получение данных о пулах
```
Handler → HyperionAPI.get_all_pools()
              ↓
         Проверка кэша (60 сек)
              ↓
         _fetch_pools_from_api() → GraphQL запрос
              ↓
         Фильтрация (TVL ≥ $100k, Volume ≥ $50k)
              ↓
         _enrich_pool() для каждого пула
              ↓
         _get_token_symbol() → token_registry.py
              ↓
         Возврат обогащенных данных
```

### 4. Навигация пользователя

**Текущий поток:**
1. `/start` → Главное меню (ReplyKeyboardMarkup)
2. "🔷 Выбрать блокчейн" → Список блокчейнов (Aptos)
3. "🔷 Aptos" → Список протоколов с данными (Hyperion)
4. "🌊 Hyperion" → Список пулов с фильтрами
5. Inline кнопки → Фильтрация, сортировка, обновление

## Технологии

- **Telegram Bot Framework:** aiogram 3.4.1
- **База данных:** SQLAlchemy 2.0.25, aiosqlite 0.19.0
- **HTTP клиент:** aiohttp 3.9.1
- **Конфигурация:** pydantic-settings 2.1.0
- **Логирование:** loguru 0.7.2
- **Миграции:** alembic 1.13.1 (не используется в текущей версии)

## Внешние API

### Hyperion GraphQL API
- **URL:** `https://hyperfluid-api.alcove.pro/v1/graphql`
- **Метод:** POST
- **Query:** GetAllPools (getPoolStat)
- **Данные:** TVL, Volume, Fees, APR, Fee Tier, токены

### Aptos GraphQL Indexer (не используется напрямую)
- **URL:** `https://api.mainnet.aptoslabs.com/v1/graphql`
- Использовался для парсинга токенов (заменен на token_registry)

## Кэширование

- **Тип:** In-memory кэш в HyperionAPI
- **TTL:** 60 секунд
- **Область:** Все пулы
- **Обновление:** Принудительное через `force_refresh=True`

## Фильтрация данных

**Автоматическая фильтрация в `get_all_pools()`:**
- TVL ≥ $100,000
- Volume 24H ≥ $50,000

**Фильтрация в `filter_pools()`:**
- min_tvl (по умолчанию $100,000)
- min_volume (по умолчанию $50,000)
- fee_tiers (список fee rates)
- has_farm (True/False/None)
- sort_by (tvl/volume/apr/fees)
- limit (максимум результатов)

## Обработка ошибок

- Try/except блоки во всех handlers
- Логирование через loguru
- Возврат старых данных из кэша при ошибках API
- Пользовательские сообщения об ошибках

## Особенности реализации

1. **Постоянное меню:** ReplyKeyboardMarkup всегда видно внизу экрана
2. **Inline клавиатуры:** Для фильтров, сортировки, навигации
3. **Кэширование:** 60 секунд для снижения нагрузки на API
4. **Обогащение данных:** Добавление вычисляемых полей (token_a, token_b, fee_tier_display, total_apr)
5. **Реестр токенов:** Централизованное хранение известных токенов
6. **Многоуровневая навигация:** Блокчейн → Протокол → Пулы

## Будущие улучшения

1. **Другие блокчейны:** Добавление поддержки других блокчейнов
3. **База данных:** Использование БД для кэширования пулов
4. **Уведомления:** Отслеживание изменений APR
5. **Аналитика:** Сохранение исторических данных

