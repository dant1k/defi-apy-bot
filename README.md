# DeFi APY Bot - Telegram бот для анализа пулов на Aptos

Telegram бот для анализа DeFi пулов ликвидности на блокчейне Aptos. Помогает находить лучшие пулы по APR, TVL и другим метрикам.

## Возможности

- 🔍 Поиск пулов по минимальному APR
- 📊 Топ пулов по APR с фильтрацией
- 💰 Информация о TVL, Volume 24h, APR (fees + farming)
- ⚙️ Настройка фильтров (в разработке)
- 📈 Отслеживание пулов (в разработке)

## Установка

1. **Клонируйте репозиторий** (если есть) или используйте существующую структуру

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp .env.example .env
```

Откройте `.env` и укажите:
```
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=sqlite+aiosqlite:///./data/bot.db
APTOS_NETWORK=mainnet
APTOS_GRAPHQL_URL=https://api.mainnet.aptoslabs.com/v1/graphql
```

5. **Создайте необходимые директории:**
```bash
mkdir -p data logs
```

## Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Вставьте токен в файл `.env`

## Запуск

```bash
python -m bot.main
```

Или если вы в корне проекта:
```bash
cd defi-apy-bot
python -m bot.main
```

## Команды бота

- `/start` - Начало работы, показ главного меню
- `/pools` - Показать топ 10 пулов по APR
- `/find [apr]` - Найти пулы с минимальным APR
  - Пример: `/find 20` - найдет пулы с APR > 20%

## Структура проекта

```
defi-apy-bot/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Главный файл запуска бота
│   ├── handlers/            # Обработчики команд и сообщений
│   │   ├── start.py         # Команда /start
│   │   ├── pools.py         # Команда /pools
│   │   └── strategies.py    # Команда /find
│   ├── adapters/            # Адаптеры для получения данных
│   │   ├── base.py          # Базовый класс адаптера
│   │   └── hyperion.py      # Адаптер для Hyperion/Aptos
│   ├── database/            # Работа с базой данных
│   │   ├── models.py        # SQLAlchemy модели
│   │   └── crud.py          # CRUD операции
│   └── utils/               # Утилиты
│       └── formatters.py    # Форматирование сообщений
├── config/
│   └── settings.py          # Настройки приложения
├── data/                    # Данные БД (создается автоматически)
├── logs/                    # Логи (создается автоматически)
├── requirements.txt         # Зависимости Python
├── .env.example             # Пример файла окружения
├── .gitignore
└── README.md
```

## Технологии

- **aiogram 3.4.1** - Асинхронный фреймворк для Telegram Bot API
- **SQLAlchemy 2.0.25** - ORM для работы с БД
- **aiohttp** - Асинхронные HTTP запросы
- **pydantic-settings** - Управление конфигурацией
- **loguru** - Логирование

## База данных

По умолчанию используется SQLite. Для продакшена рекомендуется PostgreSQL:

```
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
```

## Разработка

### Добавление новых адаптеров

Создайте новый класс, наследующий `BaseAdapter` в `bot/adapters/`:

```python
from bot.adapters.base import BaseAdapter, PoolData

class MyAdapter(BaseAdapter):
    async def get_pools(self) -> List[PoolData]:
        # Ваша логика получения данных
        pass
```

### Добавление новых команд

Создайте новый router в `bot/handlers/`:

```python
from aiogram import Router
from aiogram.types import Message
from aiogram.filters import Command

router = Router()

@router.message(Command("mycommand"))
async def cmd_mycommand(message: Message):
    await message.answer("Ответ")
```

Затем зарегистрируйте в `bot/main.py`:
```python
from bot.handlers import myhandler
dp.include_router(myhandler.router)
```

## Примечания

- Адаптер Hyperion использует упрощенную логику парсинга GraphQL событий. В реальном проекте необходимо адаптировать под конкретную структуру контракта.
- DefiLlama API используется как fallback, но может требовать дополнительной настройки endpoints.

## Лицензия

MIT

